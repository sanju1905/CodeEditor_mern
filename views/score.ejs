
<!DOCTYPE html>
<html>
<head>
  <title><%= title %></title>

  <style>
    /* Your existing CSS styles here */

    /* Additional styles for the result card */
    .result-card {
      border: 2px solid green; /* Border color for the result card */
      border-radius: 10px;
      box-shadow: 0 0 10px 2px rgba(100, 100, 100, 0.1);
      background: radial-gradient(circle, rgb(202, 205, 220) 0%, rgb(70, 194, 252) 100%);
      color: green;
      overflow: hidden;
      width: 600px;
      max-width: 100%;
      padding: 1rem;
      text-align: center; /* Center align the text in the result card */
    }
  </style>
</head>
<body>
  <h1><%= title %></h1>
  <!-- Result card to display the score -->
  <div class="result-card mt-5" style="margin-left: 300px;">
    <h2>Your Quiz Score</h2>
    <h3 class="fw-bod text-danger">You scored <%= points %> out of <%= total %> points.</h3>
      <a class="btn btn-success" href="/quiz-questions">Play Again</a>
  </div>
</body>
</html>
