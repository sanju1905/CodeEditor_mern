<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>

  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-rbs5PmCz3BYZrLl6O3oVMWSktQOp6b7tfzbs70jR+ck4pOXP5SA7FfFJc2jc" crossorigin="anonymous">

  <style>
    body {
      margin: 0;
      padding: 0;
      font-family: 'Arial', sans-serif;
    }

    .header {
      position: absolute;
      top: 0;
      left: 0;
      background-color: #f2f2f2;
      padding: 1rem;
    }

    .result-card {
      border: 2px solid green;
      border-radius: 10px;
      box-shadow: 0 0 10px 2px rgba(100, 100, 100, 0.1);
      background: radial-gradient(circle, rgb(202, 205, 220) 0%, rgb(70, 194, 252) 100%);
      color: black;
      overflow: hidden;
      padding: 1rem;
      text-align: left; /* Align text to the left */
    }

    .content {
      margin-top: 100px;
    }

    pre {
      white-space: pre-wrap;
      font-family: 'Courier New', monospace;
      background-color: #f5f5f5;
      padding: 10px;
      border-radius: 5px;
      overflow-x: auto;
    }

    code {
      font-family: inherit;
    }

    /* Additional styles for layout */
    h1 {
      text-align: center;
      color: #333; /* Dark text color */
    }

    .card-title {
      display: flex;
      align-items: center;
    }

    .card-title i {
      margin-right: 10px;
    }
  </style>
</head>

<body>

  <div class="content container">
    <h1 class="">Share your simple Questions with thousands of our developers across the World</h1>

    <div class="row row-cols-1 row-cols-md-2 g-4">
      <% ques.forEach(question => { %>
        <div class="col">
          <div class="result-card mt-5">
            <div class="card-body">
              <h2 class="card-title text-primary"><i class="bi bi-person text-success"></i>: <%= question.name %></h2>

              <div class="mt-4">
                <h4 class="card-subtitle mb-2 text-success"><u>Question:</u></h4>
                <div contenteditable="true" class="card-text code-editable"><%= question.question %></div>
              </div>

              <div class="mt-4">
                <h4 class="card-subtitle mb-2 text-success"><u>Code:</u></h4>
                <% if (question.codeContent) { %>
                  <pre contenteditable="true" class="card-text code-editable"><code><%= question.codeContent %></code></pre>
                <% } else { %>
                  <p class="card-text">No code available</p>
                <% } %>
              </div>

              <div class="mt-4">
                <h4 class="card-subtitle mb-2 text-success"><u>Video:</u></h4>
                <% if (question.video) { %>
                  <div class="embed-responsive embed-responsive-16by9">
                    <iframe class="embed-responsive-item" width="100%" src="<%= question.video %>" allowfullscreen></iframe>
                  </div>
                <% } else { %>
                  <p class="card-text">No video available</p>
                <% } %>
              </div>
            </div>
          </div>
        </div>
      <% }); %>
    </div>
  </div>

  <!-- Bootstrap JS and Popper.js -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.1/dist/js/bootstrap.bundle.min.js" integrity="sha384-rbs5PmCz3BYZrLl6O3oVMWSktQOp6b7tfzbs70jR+ck4pOXP5SA7FfFJc2jc" crossorigin="anonymous"></script>

</body>

</html>
